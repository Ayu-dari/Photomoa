default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: mizoguchiayu    # 追加
  host: localhost           # 追加

development:
  <<: *default
  database: photomoa_development

test:
  <<: *default
  database: photomoa_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
